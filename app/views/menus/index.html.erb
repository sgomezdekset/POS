<h1>Settings</h1>

<table>
  <thead>
    <tr>
      <th>Menus list:</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @menus.each do |menu| %>
      <tr>
        <td><%= menu.name %></td>
        <div class="prints">
        <td><%= link_to 'Show', menu %></td>
        <td><%= link_to 'Edit', edit_menu_path(menu) %></td>
        <td><%= link_to 'Delete', menu, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </div>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<div class="links"><%= link_to 'Add a menu', new_menu_path %> |
<a href="http://localhost:3000/">Home page</a><br>
</div>

