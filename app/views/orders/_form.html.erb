<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% @menus.each do |menu| %>
    <h3> <%= menu.name %> </h3>

    <% menu.items.each do |item| %>
      <div class="menuItem"><%= item.name %> (<%= number_to_currency item.price %>)</div>
    <% end %>

  <% end %>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
